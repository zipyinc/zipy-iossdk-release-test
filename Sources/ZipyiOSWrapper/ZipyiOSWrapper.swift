import Foundation
import SwiftProtobuf
@_exported import ZipyiOS

// This wrapper ensures SwiftProtobuf is properly linked
public func initializeWrapper() {
    // This function exists to ensure the module is properly linked
    // You don't need to call it explicitly
} 